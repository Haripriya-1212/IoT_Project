from preprocessing import convert_voc_to_yolo
import torch
from matplotlib import pyplot as plt
import numpy as np
import cv2
import itertools
import os 


def image_to_speed(image):
    # Use image and return speed or None if no speed found
    
    # USE ML MODEL TO CHECK IF FRAME HAS SPEED SIGN
    # IF NO, RETURN

    # USE OPENCV TO EXTRACT SPEED LIMIT FROM FRAME AND PUBLISH IT
        
    pass
